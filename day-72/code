def chunk_array(arr, size):
    result = [arr[i:i + size] for i in range(0, len(arr), size)]

    return result
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
def zeros(n):
    z = 0
    k = 1
    while n >= 5**k:
        z += n // 5**k
        k += 1
    return z
