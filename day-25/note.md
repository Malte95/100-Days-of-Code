Today I continued with the HashTable project and finished it.
I added the functunality to my remove and lookup methods.
The remove method takes key as an argument and compute its hash. Then it confirms if the key exists in the collection.
If so, it removes the corresponding key-value pair from the hash table. 
The lookup method also takes key as it's argument and computes it hash. 
Then it returnes the corresponding value stored inside the has table.
If the key does not exist in the collection, it returns None.
Then I read about searching and sorting algorithmes like bineary search and linear search and how they differ,
as well as how a merge sort works.
After that I started to implement my own binary search algorithm. 

