# My Java Learning Progress Today ☕

Today, I started working through a Java tutorial and learned some of the basic concepts of Java by practicing them myself.

## What I learned

* Console output with `System.out.print()` and `System.out.println()`
* Line breaks with `\n`
* Single-line comments with `//`
* Multi-line comments with `/* ... */`
* The IntelliJ shortcut `sout` + `Tab`
* Declaring and initializing variables
* Combining text and variables with `+`
* Simple conditions with `if` and `else`

## Data types I used

I worked with several Java data types:

```java
String name = "Malte";
int gta = 6;
double pi = 3.14159;
char gender = 'M';
boolean isAdmin = true;
```

I learned that:

* `String` stores text.
* `int` stores whole numbers.
* `double` stores decimal numbers.
* `char` stores a single character.
* `boolean` can be either `true` or `false`.

## Example of a condition

```java
boolean isStudent = true;

if (isStudent) {
    System.out.println("You are a student!");
} else {
    System.out.println("You are NOT a student!");
}
```

I also changed the color of my console output in IntelliJ IDEA to green (`#00FF00`).

Today, I built a good basic understanding of Java. Next, I want to continue practicing and learn how to work with user input and calculations.
