Today I finished the Linked List Project. 
Then I reviewd the Data Structures and finished the test succesfully. 
Then I began my fourth certification project: a Hash Table.
I created a class called "HashTable". The __init__ method creates an empty dictionary, for every instance of HashTable.
Then I defined four methods: hash, add, remove and lookup.
The has method creates a number using the ACII for the string given to it. 
The add method uses this number to check if this number already exists in the self.collection dictionary.
If not, it adds the number to the dictionary implements another dictionary after the number. This is called a Hash Table.
Then it adds the key to the dictionary of key_hash.

