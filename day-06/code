def divisors(integer):
    divs = set()
    for i in range(2, int(integer **0.5) +1):
        if integer % i == 0:
            divs.add(i)
            divs.add(integer // i)
    if not divs:
        return f"{integer} is prime"
    return sorted(divs)
