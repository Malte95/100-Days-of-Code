def fibonacci(n):
    sequence = [0, 1]
    if n < 0:
        return "Incorrect input"
    if n <= 1:
        return n
    for i in range(n -1):
        sequence.append(sequence[-1] + sequence[-2])
    return sequence[n]
