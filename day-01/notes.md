Today I continued with the freeCodeCamp Python Certification. I build a Planet Class in which I had to create a Class called Planet, which has 3 methods:
- __init__ withe the parameters self, name, planet_type and star
- orbit
- __str__
I also had to check if all arguments where strings and that none of the arguments are empty. In that case I had to raise either an TypeError or ValueError.
Lastly I created a few instances to test my code.
