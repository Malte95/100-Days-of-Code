Today I did the Player Interface Lab. 
I created a class called Player which inherits ABC from the abc. The __init__ method in the Player class
defindes moves, which is a list, position, which is a tuple and initiates the starting position, and path,
which is a list containing the positions. 
Then I defined the move method which stores random integers in the move attribute and then updates the position and appends it to the path list.
Then I created a class called Pawn which stores different psoitions in the moves attriubute. 
